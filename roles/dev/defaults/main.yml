github_runner:
  org: moltenbits
  org_url: https://github.com/moltenbits
  version: "2.336.0"
  sha256: 8e8839c49b7060b6b2154f4931f815df330c27f167d53ef2239ee3dfce28b079
  # One runner instance per 5 cores (10-core mini -> 2, 16-core -> 3); each lives in
  # actions-runner-<n> with its own registration and launchd service.
  count: "{{ (ansible_processor_vcpus | int / 5) | int }}"
  base_dir: "{{ home_dir }}/actions-runner"
