- name: "Disable OSX Spotlight shortcut defaults"
  shell: > 
    /usr/libexec/PlistBuddy
    -c "delete :AppleSymbolicHotKeys:64"
    -c "add :AppleSymbolicHotKeys:64:enabled bool false"
    -c "add :AppleSymbolicHotKeys:64:value:type string standard"
    -c "add :AppleSymbolicHotKeys:64:value:parameters array"
    -c "add :AppleSymbolicHotKeys:64:value:parameters: integer 32"
    -c "add :AppleSymbolicHotKeys:64:value:parameters: integer 49"
    -c "add :AppleSymbolicHotKeys:64:value:parameters: integer 1048576"
    ~/Library/Preferences/com.apple.symbolichotkeys.plist
  ignore_errors: true
   
- name: Set Raycast folder index scope
  community.general.osx_defaults:
    domain: com.raycast.macos
    key: "permissions.folders.read:{{ item }}"
    type: bool
    value: true
    state: present
  loop: "{{ raycast.folder_scopes }}"
  tags:
    - defaults

# Detect which macOS permissions Raycast lacks and walk through granting each. The grant
# step pauses for manual TCC toggles, so the whole block is tagged permissions and skipped
# by the unattended install targets — run it via `make install.permissions`.
- name: Grant Raycast the macOS permissions it needs
  tags:
    - permissions
  block:
    - name: Detect Raycast's missing macOS permissions
      ansible.builtin.include_tasks: tcc_missing_permissions.yml
      vars:
        tcc_client: com.raycast.macos
        tcc_permissions: "{{ raycast.tcc_permissions }}"

    - name: Report Raycast permissions that still need a manual grant
      ansible.builtin.debug:
        msg: "Raycast still needs granting: {{ tcc_missing_permissions | map(attribute='name') | join(', ') }}"
      when: tcc_missing_permissions | length > 0

    - name: Trigger and grant each missing permission
      when: tcc_missing_permissions | length > 0
      block:
        - name: Launch Raycast so it can service the permission-triggering deeplinks
          ansible.builtin.command:
            argv: [open, -g, -a, Raycast]
          changed_when: false

        - name: Give Raycast a moment to start
          ansible.builtin.pause:
            seconds: 3

        # Permissions sharing a deeplink (e.g. the folder permissions, all requested by one
        # file-search command) are grouped into a single trigger-and-grant prompt.
        - name: Walk through each missing grant flow one at a time
          ansible.builtin.include_tasks: raycast_grant_permission.yml
          loop: "{{ tcc_missing_permissions | groupby('deeplink') | map('last') | list }}"
          loop_control:
            loop_var: perm_group
            label: "{{ perm_names }}"
          vars:
            perm_names: "{{ perm_group | map(attribute='name') | join(', ') }}"
            perm_pane: "{{ perm_group[0].pane }}"
            perm_deeplink: "{{ perm_group[0].deeplink }}"

