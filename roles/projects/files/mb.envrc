source_up_if_exists .envrc
watch_file .env
if has op-fast; then
  # Resolve op:// secret references via op-fast (Keychain-cached, works offline)
  direnv_load op-fast run --env-file .env --no-masking -- direnv dump
else
  log_error "op-fast not installed; secrets in .env not loaded"
fi
