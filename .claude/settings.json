{
  "hooks": {
    "Notification": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '\"\\(.title // \"\")\\n\\(.message // \"Notification\")\"' | { read subtitle; read msg; grrr send --title Dotfiles --subtitle \"$subtitle\" --appId Dotfiles --reactivate \"$msg\"; }"
          }
        ]
      }
    ]
  }
}
